define( [], function() {
  
  ko.builderFieldRenderingAgents = {};
  
  
  
  /**
   * The builder binding takes a in instance of a Builder type object as a binding and, using rendering agents 
   * registered for the various field types, renders a form appropriate for the builder
   */
  ko.bindingHandlers.builder = {
      
      
      
  };
  
} );